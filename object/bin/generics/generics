Generics(JDK 1.5v 도입)
    목적
        1. 타입 안정성 제공
        2. 타입 체크와 형 변환 생략 가능을 통해 코드의 간결성 유지

    사용 기호
    - T : type variable
    - E : element
    - K : key
    - V : value

    Box<T> : 제네릭 클래스
    - T Box라고 읽음
    - T : 타입 변수
    - Box : 원시 타입(raw type)

    제한된 제네릭 클래스
        ex) <T extends Fruit> : Fruit의 자손만 타입으로 사용 가능

    와일드 카드
        ex) <? extends T> : T와 그 자손들만 가능
            <? super T> : T와 그 조상들만 가능
            <?> : 제한 없음(모든 타입 가능)
        